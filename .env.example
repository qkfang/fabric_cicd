# ──────────────────────────────────────────────────────────────────
# .env.example — Copy to .env and fill in your values
# ──────────────────────────────────────────────────────────────────

# ── Service Principal credentials (per-environment) ──────────────
# Each environment uses its own SP for least-privilege isolation.
# The deploy script reads <ENV>_TENANT_ID, <ENV>_CLIENT_ID, <ENV>_CLIENT_SECRET
# based on TARGET_ENVIRONMENT, with fallback to the generic FABRIC_* vars.

# DEV Service Principal
DEV_TENANT_ID=<your-dev-tenant-id>
DEV_CLIENT_ID=<your-dev-service-principal-client-id>
DEV_CLIENT_SECRET=<your-dev-service-principal-client-secret>

# QA Service Principal
QA_TENANT_ID=<your-qa-tenant-id>
QA_CLIENT_ID=<your-qa-service-principal-client-id>
QA_CLIENT_SECRET=<your-qa-service-principal-client-secret>

# PROD Service Principal
PROD_TENANT_ID=<your-prod-tenant-id>
PROD_CLIENT_ID=<your-prod-service-principal-client-id>
PROD_CLIENT_SECRET=<your-prod-service-principal-client-secret>

# ── Fallback (shared) credentials ────────────────────────────────
# Used only if the per-environment vars above are not set.
# FABRIC_TENANT_ID=<your-azure-ad-tenant-id>
# FABRIC_CLIENT_ID=<your-service-principal-client-id>
# FABRIC_CLIENT_SECRET=<your-service-principal-client-secret>

# ── Target workspace ──────────────────────────────────────────────
TARGET_WORKSPACE_ID=<fabric-workspace-guid>
TARGET_ENVIRONMENT=DEV                 # DEV | QA | PROD

# ── Repository path ──────────────────────────────────────────────
REPO_DIR=./workspace

# ── Optional overrides ───────────────────────────────────────────
# ITEMS_IN_SCOPE=Notebook,DataPipeline,SemanticModel,Report,Environment,Lakehouse
# CLEAN_ORPHANS=false
