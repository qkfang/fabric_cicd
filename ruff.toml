# Ruff linter configuration
# Docs: https://docs.astral.sh/ruff/configuration/

line-length = 120
target-version = "py310"

[lint]
select = ["E", "F", "W", "I"]   # pycodestyle, pyflakes, isort
ignore = ["E501"]                # line-length exceptions for log messages
